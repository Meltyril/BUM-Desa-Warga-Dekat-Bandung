riemyon.
riemyon
In voice

riemyon. [HAMU],  — 19/11/2025 16:11
Image
riemyon. [HAMU],  — 23/11/2025 19:45
Desain website ini mengadopsi pendekatan minimalis dan clean yang sangat sesuai dengan prinsip UI/UX modern. Saya memilih gaya desain ini karena target audiensnya adalah masyarakat desa yang membutuhkan informasi yang mudah diakses dan tidak membingungkan. Penggunaan whitespace yang generous membantu mata pengguna fokus pada konten penting tanpa distraksi berlebihan.

Strive for Consistency
Semua tombol menggunakan bentuk rounded yang sama dengan warna hitam (#000000 atau dark gray). Tipografi menggunakan font serif yang elegan dan konsisten di seluruh halaman

Enable Frequent Users to Use Shortcuts
Meskipun ini website informasional, navigation bar yang sticky memungkinkan pengguna berpindah cepat antar halaman tanpa harus scroll ke atas

Offer Informative Feedback
Form contact pada halaman "Hubungi Kami" memiliki struktur yang jelas dengan label untuk setiap field. Button states (hover, active) memberikan feedback visual saat pengguna berinteraksi.

Design Dialog to Yield Closure
Setiap task flow memiliki akhir yang jelas. Misalnya pada form kontak: user mengisi form → klik "KIRIM PESAN" → mendapat konfirmasi. Pada halaman produk: user melihat produk → klik "DETAIL PRODUK" → mendapat informasi lengkap. 

Offer Simple Error Handling
Form dirancang dengan validation yang jelas. Required fields ditandai dengan jelas, dan struktur form yang sederhana meminimalkan kemungkinan error. 

Permit Easy Reversal of Actions
Navigation yang jelas memungkinkan pengguna kembali ke halaman sebelumnya dengan mudah.

Support Internal Locus of Control
Pengguna merasa in control karena navigation yang predictable dan transparan. 

Reduce Short-term Memory Load
Information architecture dirancang dengan chunking yang baik. Homepage memberikan overview semua section utama, sehingga user tidak perlu mengingat banyak informasi.
Halaman Home dirancang sebagai halaman landing yang memberikan gambaran komprehensif mengenai BUM Desa.
Bagian produk unggulan ditampilkan menggunakan kartu produk dengan gambar berkualitas dan hierarki informasi yang jelas untuk menarik perhatian pengguna. Bagian berita menampilkan pembaruan terbaru disertai gambar mini dan cuplikan teks untuk meningkatkan keterlibatan. Bagian ajakan bertindak di bagian bawah halaman dirancang dengan warna latar yang kontras agar lebih menonjol.

Halaman About Us menekankan penceritaan tentang perjalanan BUM Desa melalui timeline yang terstruktur dari tahun ke tahun. Carousel pengurus menampilkan profil secara ringkas tanpa membuat halaman terlalu panjang. Daftar dokumen legalitas disusun rapi dan dilengkapi tombol unduh untuk memudahkan akses. Palet warna profesional memperkuat citra terpercaya dan formal.

Halaman Products dilengkapi sistem filter kategori yang intuitif dengan tombol pil dan penanda aktif yang jelas. Grid produk responsif menampilkan gambar, kategori, harga, dan tombol detail secara konsisten. Pada halaman detail, gambar produk ditampilkan besar dengan galeri kecil di bawahnya, disertai harga yang menonjol dan deskripsi yang mudah dibaca. Informasi pemesanan hadir dengan ikon untuk membantu pemindaian cepat.

Halaman Services Setiap layanan disajikan dalam layout dua kolom dengan pola berselang-seling untuk menjaga dinamika visual. Gambar dan deskripsi disusun seimbang, dilengkapi daftar manfaat dan ikon representatif. Badge warna lembut membantu membedakan kategori layanan. CTA “Hubungi Kami” ditempatkan di bagian akhir dengan warna kontras untuk meningkatkan konversi.

Halaman News Halaman News menampilkan artikel dalam grid yang responsif dengan kartu yang memuat gambar, judul, tanggal, dan cuplikan teks. Halaman detail artikel menggunakan panjang baris optimal, tipografi nyaman dibaca, serta breadcrumb untuk memudahkan navigasi. Gambar utama dibuat lebar untuk memberikan dampak visual yang kuat dan meningkatkan keterlibatan.

Halaman Contact Halaman Contact memprioritaskan kemudahan komunikasi melalui layout dua kolom. Formulir pesan dibuat bersih dengan label jelas, sementara kolom kanan menampilkan kartu kontak staf, email, dan jam operasional. Tombol WhatsApp mengambang mendukung akses cepat. Peta lokasi Google Maps ditampilkan penuh agar pengguna dapat mengenali lokasi dengan mudah.
riemyon. [HAMU],  — 23/11/2025 20:25
-jelasin 8 golden rules nya di tiap page
-kasih gambar di 8 golden rules
-use case ditaruh di 3.3
-buat sequence diagram (hubungan ke backend nya juga)
riemyon. [HAMU],  — 02/12/2025 16:33
좋아요~ 좋아요~ 물걸레질 좋아요오~
흐으아악! 스피키 머리 잡아당기지 마세요! 
riemyon. [HAMU],  — 03/12/2025 21:16
ｗｗｗ

おいっす oissu
おじゃ ojiya
おじゃああ
あぶね abune
おしい oshii
ナイス naisu
つよ tsuyo
はやすぎ hayasugi
よわすぎ yowasugi
いける？ ikeru?
うん un
おいｗ oi
あ a
え e
あちゃちゃ achacha uh oh
うおおお uoooo
うわぁ
うわああああ whoaa
かわいい kawaii
うわうまあああ uwa umaa



！
？
ー
～ 
riemyon. [HAMU],  — 16/02/2026 22:59
Uh → あー、えー･･･
Hmm … → ふーむ･･･
Like … → なんか、その･･･
Well … → まあ、さて･･･
I mean … → つまり･･･
You know … → ほら、あの･･･
Honestly … → 正直･･･
Basically … → 要するに
Actually … → 実は･･･
Let me see … → えっとね･･･
You know what?  → あのさ〜
Long story short … → 手短に言うと･･･
How should I put it?  → なんて言えばいいかな･･･
riemyon. [HAMU],  — 26/02/2026 18:26
Image
riemyon. [HAMU],  — 01/03/2026 22:12
Image
riemyon. [HAMU],  — 15/03/2026 00:31
(w$FF63555DFFEAA8F0FFF6FAF0)

The above data starting at '(' and ending at ')' is the color string you give to PalMod to copy a color, as in (""0000) . PalMod handles this automatically on CTRL+C /CTRL+V, but you're seeing 'secret' extra data right now using clipboard tricks. The following data is additional debug information useful for ROM hacking:

This palette begins in the ROM at location:
    0x410
The current selection begins at ROM location:
    0x604
PalMod's version of the data in the ROM at that location reads:
    5D 55 63 FF F0 A8 EA FF F0 FA F6 FF 
The French Bread RGB version of this data is:
    rgb = [ [ 93,85,99 ], [ 240,168,234 ], [ 240,250,246 ] ],


You can turn off this secret extended data by going to PalMod's Settings menu.
riemyon. [HAMU],  — 21/03/2026 22:05
e1a1ff - ff92ec
riemyon. [HAMU],  — 22/03/2026 22:01
Image
riemyon. [HAMU],  — 20:12
// server.js
require('dotenv').config();
const express = require('express');
const cors = require('cors');
const path = require('path');
const { pool } = require('./db');

message.txt
8 KB
﻿
Aste.
astmuyagi
 
i can't be happy, afterall.
// server.js
require('dotenv').config();
const express = require('express');
const cors = require('cors');
const path = require('path');
const { pool } = require('./db');

const app = express();
// Global middleware
app.use(cors());
app.use(express.json());

// Logger sederhana (lihat setiap request di terminal)
app.use((req, _res, next) => {
  console.log(`[req] ${req.method} ${req.url}`);
  next();
});

// Health & root routes (buat tes cepat)
// ⬇️ Ganti '/' jadi '/status' agar root ('/') bisa dipakai untuk SPA frontend
app.get('/status', (_req, res) => {
  res.send('Backend is running 🚀');
});

app.get('/health', (_req, res) => {
  res.json({ status: 'ok' });
});

/* ======== [ADD] NEWS ROUTES (2 baris) ======== */
const newsRoutes = require('./routes/news.routes');
app.use('/api/news', newsRoutes);
/* ============================================ */

/* ======== [ADD] ADMIN ROUTES (BARU) ======== */
const adminRoutes = require('./routes/admin.routes');
app.use('/api/admin', adminRoutes);
/* ========================================== */

// =======================
// PRODUCTS (MySQL)
// =======================

// CREATE ( di simpan ke MySQL)
app.post('/api/products', async (req, res) => {
  try {
    const { name, price, description, stock } = req.body;
    if (!name || price == null) {
      return res.status(400).json({ error: 'name dan price wajib diisi' });
    }
    const p = {
      name: String(name).trim(),
      price: Number(price),
      description: description ?? null,
      stock: Number.isFinite(Number(stock)) ? Number(stock) : 0,
    };

    const [result] = await pool.execute(
      'INSERT INTO products (name, price, description, stock) VALUES (?,?,?,?)',
      [p.name, p.price, p.description, p.stock]
    );
    const [rows] = await pool.execute('SELECT * FROM products WHERE id = ?', [result.insertId]);
    return res.status(201).json({ data: rows[0] });
  } catch (err) {
    console.error('[mysql:create]', err);
    return res.status(500).json({ error: 'Gagal membuat produk' });
  }
});

// READ (list + query sederhana ?q=&page=&limit=)
app.get('/api/products', async (req, res) => {
  try {
    const q = (req.query.q || '').toString().trim();
    const limit = Math.max(1, Math.min(50, Number(req.query.limit) || 10));
    const page  = Math.max(1, Number(req.query.page) || 1);
    const offset = (page - 1) * limit;

    let where = '';
    let params = [];
    if (q) {
      where = 'WHERE name LIKE ? OR description LIKE ?';
      params = [`%${q}%`, `%${q}%`];
    }

    const [[{ total }]] = await pool.query(
      `SELECT COUNT(*) AS total FROM products ${where}`,
      params
    );
    const [rows] = await pool.query(
      `SELECT * FROM products ${where} ORDER BY id DESC LIMIT ? OFFSET ?`,
      [...params, limit, offset]
    );

    return res.json({ meta: { total, page, limit }, data: rows });
  } catch (err) {
    console.error('[mysql:list]', err);
    return res.status(500).json({ error: 'Gagal mengambil produk' });
  }
});

// READ (detail by id)
app.get('/api/products/:id', async (req, res) => {
  try {
    const id = Number(req.params.id);
    const [rows] = await pool.execute('SELECT * FROM products WHERE id = ?', [id]);
    if (rows.length === 0) {
      return res.status(404).json({ error: 'Produk tidak ditemukan' });
    }
    return res.json({ data: rows[0] });
  } catch (err) {
    console.error('[mysql:detail]', err);
    return res.status(500).json({ error: 'Gagal mengambil produk' });
  }
});

// UPDATE (PUT /api/products/:id)
app.put('/api/products/:id', async (req, res) => {
  try {
    const id = Number(req.params.id);
    const { name, price, description, stock } = req.body;

    // ambil data lama
    const [existing] = await pool.execute('SELECT * FROM products WHERE id = ?', [id]);
    if (!existing.length) return res.status(404).json({ error: 'Produk tidak ditemukan' });

    const prev = existing[0];
    const next = {
      name: (name ?? prev.name).toString().trim(),
      price: price != null ? Number(price) : prev.price,
      description: description ?? prev.description,
      stock: stock != null ? Number(stock) : prev.stock,
    };

    if (!next.name || next.price == null) {
      return res.status(400).json({ error: 'name dan price wajib diisi' });
    }

    const [result] = await pool.execute(
      `UPDATE products
       SET name=?, price=?, description=?, stock=?
       WHERE id=?`,
      [next.name, next.price, next.description, next.stock, id]
    );

    if (result.affectedRows === 0) {
      return res.status(404).json({ error: 'Produk tidak ditemukan' });
    }

    const [rows] = await pool.execute('SELECT * FROM products WHERE id=?', [id]);
    return res.json({ data: rows[0] });
  } catch (err) {
    console.error('[mysql:update]', err);
    return res.status(500).json({ error: 'Gagal mengubah produk' });
  }
});

// DELETE (DELETE /api/products/:id)
app.delete('/api/products/:id', async (req, res) => {
  try {
    const id = Number(req.params.id);
    const [result] = await pool.execute('DELETE FROM products WHERE id = ?', [id]);
    if (result.affectedRows === 0) {
      return res.status(404).json({ error: 'Produk tidak ditemukan' });
    }
    return res.json({ ok: true });
  } catch (err) {
    console.error('[mysql:delete]', err);
    return res.status(500).json({ error: 'Gagal menghapus produk' });
  }
});


// LOKASI / MAPS (Footer)
app.get('/api/location/business', (_req, res) => {
  const name    = process.env.BUSINESS_NAME || 'Lokasi';
  const address = process.env.BUSINESS_ADDRESS || '';
  const lat     = Number(process.env.BUSINESS_LAT);
  const lng     = Number(process.env.BUSINESS_LNG);

  if (!Number.isFinite(lat) || !Number.isFinite(lng)) {
    return res.status(500).json({ error: 'Koordinat bisnis belum di-set di .env' });
  }

  // ✅ Klik: buka Google Maps tepat ke koordinat (format Maps URLs, api=1 wajib)
  const mapsUrl = `https://www.google.com/maps/search/?api=1&query=${lat},${lng}`; // [web:98]

  // ✅ Klik: petunjuk arah ke koordinat
  const directionsUrl = `https://www.google.com/maps/dir/?api=1&destination=${lat},${lng}`; // [web:98]

  // ✅ Tampilan gambar peta (iframe) tanpa API key
  // Catatan: ini bukan Maps Embed API resmi, tapi biasanya paling “langsung jadi”.
  const embedUrl = `https://maps.google.com/maps?q=${lat},${lng}&t=m&z=16&output=embed`; // [web:125]

  res.json({ name, address, lat, lng, mapsUrl, directionsUrl, embedUrl });
});

// =======================
// [NEW] SERVE FRONTEND BUILD (SPA)
// =======================
// Lokasi folder build frontend (bisa diubah via env FRONTEND_DIST)
const FRONTEND_DIST = process.env.FRONTEND_DIST || path.join(__dirname, 'dist');

// Serve file statis dari build frontend
app.use(express.static(FRONTEND_DIST));

// ✅ Fallback baru yang kompatibel Express 5
app.get(/^\/(?!api).*/, (req, res) => {
  res.sendFile(path.join(FRONTEND_DIST, 'index.html'));
});

// 404 & Error handlers
// Handler 404 khusus API (karena route SPA di atas sudah menangani non-API)
app.use((req, res) => {
  res.status(404).json({ error: `Route ${req.method} ${req.originalUrl} tidak ditemukan` });
});

app.use((err, _req, res, _next) => {
  console.error('[error]', err);
  res.status(500).json({ error: 'Internal server error' });
});

// Start server
const PORT = process.env.PORT || 5000;
app.listen(PORT, () => {
  console.log(`[server] listening on http://localhost:${PORT}`);
  console.log(`[server] serving frontend from: ${FRONTEND_DIST}`);
});
message.txt
8 KB
